export enum Kind {
    Media = 1,
    AudioRecording = 2,
    SpeechRecognition = 3,
    Network = 10,
}

export enum Status {
    Undetermined = 1,
    GrantedAll = 2,
    GrantedLimited = 3,
    Denied = 4,
}
