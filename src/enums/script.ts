export enum Status {
    Published = 'published',
    Publishing = 'publishing',
    Rejected = 'rejected',
    Draft = 'draft',
}

export enum SceneKindKey {
    Search = 'search',
    Published = 'published',
    Collect = 'collect',
}

export enum SceneKind {
    Published = 'published',
    Collect = 'collect',
}

export enum ApiRoleArrangement {
    LetMe = 1,
    LetPlayer = 2,
}
